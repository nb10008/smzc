sampler g_diffuseMap : register(s0);
float4 main(float2 uv: TEXCOORD0, float diffuse: TEXCOORD1):COLOR
{
    float4 ret = 0;
    ret.x = saturate(diffuse);
    ret.w = tex2D(g_diffuseMap,uv).a;
    return ret;
}